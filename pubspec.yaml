name: yours_technical_test
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  another_flushbar: ^1.12.30
  collection: ^1.18.0
  copy_with_extension: ^5.0.4
  equatable: ^2.0.5
  flutter_bloc: ^8.1.4
  intl: ^0.18.1
  meta: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.8
  copy_with_extension_gen: ^5.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/
